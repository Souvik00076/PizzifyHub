<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&family=Roboto&display=swap" rel="stylesheet">
<link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
    <link href="/css/app.css" rel="stylesheet">
</head>
<body>
    <!--First section which is a nav-->
    
    <nav class="container mx-auto flex items-center justify-between py-4">
        <!-- 
        Container : Creating a container, by default not center
        mx-auto : Puts everything at the center
        flex  items-center : items are aligned center vertically
        justify-between : same as in CSS
        py-4 : padding in top and bottom 4 -> 16 em
        -->
        <div>
            <img src="/img/logo.png" alt="logo">
        </div>

        <div>
            <ul class="flex items-center">
                <!--
                    flex items-center : items are aligned center vertically
                -->
            <li class="ml-6"><a href="#">Menu</a></li>
            <li class="ml-6"><a href="#">Offers</a></li>
            <li class="ml-6"><a href="/auth/register">Register</a></li>
            <li class="ml-6"><a href="/auth/login">Login</a></li>
            <li class="ml-6  rounded-full flex items-center">
                <span id="cartCounter" class="text-white font-bold ml-6"><%=session.cart? session.cart.totalQty: ' '%></span>
                <a href="#"><img src="/img/cart.png" alt="cart-logo" class="inline-block px-2 py-1"></a></li>     
                <!--
                    session not defined error..
                    To resolve this use glbal middleware 
                    inline-block : 
                    px:width in left & right
                    py : padding in y direction both top & bottom
                    px : padding in x direction both left & right
                    rounded-full : appearing rounded
                    w : width
                    h : height
                    ml : margint left
                -->
        </ul>
        </div>
    </nav>
    <%- body %>
  
  
    <script src="/js/app.js"></script>
</body>
</html>